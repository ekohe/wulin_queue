source "https://rubygems.org"

# Specify your gem's dependencies in wulin_queue.gemspec
gemspec

# The gemspec allows any 1.4-or-later Solid Queue; pin the test bundle to the
# version host apps are actually running, so the suite verifies what ships.
# Bumping this is how you check compatibility with a newer Solid Queue.
gem "solid_queue", "1.4.0"
